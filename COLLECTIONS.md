# What this evidence rests on: hr

Generated by `pnpm collections` from the committed evidence.

Every collection below was read once and its testimony written into `attestations/`, along with
the number of playable tokens it held. That total is the denominator of every rate, and recording
it is what makes the downloaded text **disposable**: a later build that adds a family reuses these
lines instead of re-reading gigabytes to rediscover them.

So the dumps are not kept. This table is the pointer back, and a rebuild that wants to re-read a
collection rather than reuse its record only has to put the file back where the path says.

| collection | tokens | what it is | where it came from |
| --- | ---: | --- | --- |
| `fw2` | 1,144,091,583 | FineWeb-2 Croatian — a web crawl nobody here made | [fineweb2-hrv.parquet](https://huggingface.co/datasets/HuggingFaceFW/fineweb-2/resolve/main/data/hrv_Latn/train/000_00000.parquet) |
| `wiki:hr` | 56,904,056 | Croatian Wikipedia — modern encyclopedic prose | [hrwiki.xml.bz2](https://dumps.wikimedia.org/hrwiki/latest/hrwiki-latest-pages-articles.xml.bz2) |
| `ia` | 45,918,871 | Internet Archive Croatian books — literature, and the register a newspaper never reaches | [archive-hr](https://archive.org/search?query=mediatype%3Atexts+AND+%28language%3A%22Croatian%22+OR+language%3A%22hrv%22%29) |
| `wikisource:hr` | 7,213,780 | Croatian Wikisource — same Wikimedia family, so it corroborates rather than counts | [hrwikisource.xml.bz2](https://dumps.wikimedia.org/hrwikisource/latest/hrwikisource-latest-pages-articles.xml.bz2) |
| `lz:hrv_newscrawl_2016_1M` | 951,782 | Leipzig hrv_newscrawl_2016_1M — modern news, cited by the page each sentence came from | [hrv_newscrawl_2016_1M](https://downloads.wortschatz-leipzig.de/corpora/hrv_newscrawl_2016_1M.tar.gz) |
| `lz:hrv_news_2020_1M` | 763,066 | Leipzig hrv_news_2020_1M — modern news, cited by the page each sentence came from | [hrv_news_2020_1M](https://downloads.wortschatz-leipzig.de/corpora/hrv_news_2020_1M.tar.gz) |
| `lz:hrv_news_2019_300K` | 549,623 | Leipzig hrv_news_2019_300K — modern news, cited by the page each sentence came from | [hrv_news_2019_300K](https://downloads.wortschatz-leipzig.de/corpora/hrv_news_2019_300K.tar.gz) |
| `ebible:hrv` | 468,323 | Šarić's Bible of 1942 — a family nothing else here belongs to | [hrv_vpl.txt](https://ebible.org/Scriptures/hrv_vpl.zip) |
| `web:index.hr` | 166,070 |  | fetched from `index.hr`, recorded in `searched.tsv` |
| `web:net.hr` | 134,064 |  | fetched from `net.hr`, recorded in `searched.tsv` |
| `web:novilist.hr` | 107,545 |  | fetched from `novilist.hr`, recorded in `searched.tsv` |
| `web:jutarnji.hr` | 32,801 |  | fetched from `jutarnji.hr`, recorded in `searched.tsv` |
| `web:glas-slavonije.hr` | 30,079 |  | fetched from `glas-slavonije.hr`, recorded in `searched.tsv` |
| `web:vecernji.hr` | 28,582 |  | fetched from `vecernji.hr`, recorded in `searched.tsv` |
| `tat` | 25,538 | Tatoeba Croatian — contemporary and conversational | [hrv_sentences.tsv](https://downloads.tatoeba.org/exports/per_language/hrv/hrv_sentences.tsv.bz2) |
| `web:tportal.hr` | 20,515 |  | fetched from `tportal.hr`, recorded in `searched.tsv` |
| `web:srce.hr` | 16,515 |  | fetched from `srce.hr`, recorded in `searched.tsv` |
| `web:booksa.hr` | 4,201 |  | fetched from `booksa.hr`, recorded in `searched.tsv` |
| `web:slobodnadalmacija.hr` | 2,982 |  | fetched from `slobodnadalmacija.hr`, recorded in `searched.tsv` |
| `web:hrt.hr` | 456 |  | fetched from `hrt.hr`, recorded in `searched.tsv` |

Totals are playable tokens after folding, not bytes. A collection missing from this table was
never scanned; one present with no link was fetched by hand and its path is given instead.
